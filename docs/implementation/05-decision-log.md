# Decision Log

This file records decisions and minimum assumptions made during the Stage 0 review. Product decisions not explicitly confirmed remain marked as requiring confirmation.

| ID | Decision | Reason | Basis | Alternatives | Impact | Needs User Confirmation |
|---|---|---|---|---|---|---|
| D-001 | Treat this turn as documentation review and planning only. | The current instruction explicitly forbids business implementation, framework initialization, dependency installation, database creation, Docker, commits, and pushes. | Current task instruction. | Start Stage 1 implementation immediately. | Keeps repository within allowed scope. | No |
| D-002 | Apply root `AGENTS.md` as the repository constitution. | The repository now has `AGENTS.md` at the root after the user moved it from `docs/`. | Repository inspection; current instruction; `AGENTS.md`. | Keep a duplicate under `docs/`. | Aligns future agent startup with the requested root instruction file. | No |
| D-003 | Record missing `README.md` as a repository baseline gap, not a Stage 0 blocker. | Stage 0 says README is part of final GitHub display in Stage 8; current instruction asked to read it, but it is absent. | Repository inspection; baseline Stage 8. | Create README now. | Avoids premature Stage 8 documentation work in this review-only turn. | Yes, if README should be created before Stage 8 |
| D-004 | Preserve the stage names and numbering from `01-stage0-baseline.md`. | The user instructed not to change stage numbering/names if the baseline already defines them. | Current instruction; baseline section 18. | Redesign stages by module or architecture layer. | Roadmap aligns with frozen baseline. | No |
| D-005 | For planning, mark Java version as unresolved and recommend confirmation before Spring Boot initialization. | `AGENTS.md` says Java 21; stage 0 says Java 17. This affects project setup. | `AGENTS.md` section 6; baseline section 7. | Silently choose Java 21 or Java 17. | Prevents incorrect project initialization. | Yes |
| D-006 | For planning, keep V1 authentication as simulated `X-User-Id` and no JWT/Spring Security unless baseline changes. | Stage 0 explicitly bans login/JWT/Spring Security/RBAC; adding them would expand MVP scope. | Baseline sections 4 and 6; requirements section 3.3. | Follow `AGENTS.md` JWT item. | Keeps MVP narrow and aligned with stage 0, but conflict remains recorded. | Yes, because `AGENTS.md` conflicts |
| D-007 | Do not require Pinia in V1 tasks. | Baseline says first version does not force Pinia; `AGENTS.md` lists Pinia. | Baseline section 14; `AGENTS.md` section 6. | Use Pinia by default. | Avoids unnecessary frontend scope; can add later if a concrete state-management need appears. | No, unless user wants Pinia |
| D-008 | Keep database table count fixed at eight for V1. | Baseline and database design explicitly forbid adding tables. | Baseline section 8; database design section 2. | Add tables for trace, cache, auth, or imports. | Prevents scope expansion; trace stays response-only/logical, not a table. | No |
| D-009 | Plan explicit XML mapper SQL for required joins. | Baseline requires actual `INNER JOIN` and `LEFT JOIN`; MyBatis-Plus wrappers alone may not satisfy this. | Baseline section 9; database design section 6. | Use only MyBatis-Plus generated queries. | Ensures acceptance can verify required SQL semantics. | No |
| D-010 | Treat complete DTO/VO schemas as module-level implementation decisions to freeze before each module task. | API document provides DTO/VO names and examples but not every field for every endpoint. | API sections 12 and 13. | Attempt to freeze all fields in this Stage 0 review. | Keeps this review focused and avoids inventing product details prematurely. | No, unless user wants full API schema freeze before coding |
| D-011 | Make Stage 1 first task a foundation preflight/skeleton task, not business implementation. | Baseline says Stage 1 first task is environment confirmation, directory/Git, Spring Boot empty project, startup verification; current task says repo/Git already exist and forbids this turn from initializing. | Baseline section 21; current instruction. | Start knowledge CRUD or Agent flow first. | Maintains stage gate order. | No |
| D-012 | Do not create `backend/`, `frontend/`, or `deploy/` directories in this turn. | Current instruction explicitly forbids initializing complete Spring Boot, Vue, database, or Docker project and says only document suggested structure. | Current instruction. | Create empty directories for appearance. | Avoids misleading empty scaffolding. | No |
| D-013 | Model-log statistics remain lower priority than log writing and paginated query. | API document states statistics priority is lower. | API section 10. | Implement statistics before logs are reliable. | Keeps Stage 6 focused on required observability first. | No |
| D-014 | Health check must not call the LLM. | API document states this to avoid cost and latency. | API section 11. | Probe the LLM from health. | Reduces cost and failure blast radius. | No |
| D-015 | Recommended first Stage 1 task is `S1-T01`. | It resolves foundation assumptions and creates only backend skeleton after approval. | Baseline Stage 1 and this implementation plan. | Start database schema or common response first. | Prevents Java/auth conflicts from infecting project setup. | Yes, for starting Stage 1 |
